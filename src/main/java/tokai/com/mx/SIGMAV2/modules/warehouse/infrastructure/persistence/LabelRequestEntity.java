package tokai.com.mx.SIGMAV2.modules.warehouse.infrastructure.persistence;

// Entidad temporal deshabilitada para evitar conflictos
// Esta debería estar en el módulo de etiquetas/marbetes cuando se implemente

/*
@Entity
@Table(name = "label_requests")
@Data
public class LabelRequestEntity {
    
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @ManyToOne
    @JoinColumn(name = "id_warehouse")
    private WarehouseEntity warehouse;
    
    // Otros campos...
}
*/

public class LabelRequestEntity {
    // Clase placeholder - implementar en el módulo de etiquetas/marbetes
}