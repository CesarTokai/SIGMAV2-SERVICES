package tokai.com.mx.SIGMAV2.modules.warehouse.infrastructure.persistence;

// Entidad temporal deshabilitada para evitar conflictos
// Esta debería estar en el módulo de inventario cuando se implemente

/*
@Entity
@Table(name = "inventory_stock")
@Data
public class InventoryStockEntity {
    
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @ManyToOne
    @JoinColumn(name = "id_warehouse")
    private WarehouseEntity warehouse;
    
    // Otros campos...
}
*/

public class InventoryStockEntity {
    // Clase placeholder - implementar en el módulo de inventario
}